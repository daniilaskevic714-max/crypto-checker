# Release Checksums

For every release, publish SHA-256 hashes for release archives.

Example:

```bash
sha256sum crypto-checker-v1.0.0.zip
sha256sum crypto-checker-v1.0.0.tar.gz
```

Users can verify downloads:

```bash
sha256sum -c SHA256SUMS.txt
```
